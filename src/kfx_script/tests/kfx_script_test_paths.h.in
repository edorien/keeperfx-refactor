// Generated by CMake (configure_file(), same idea ver_defs.h.in already
// uses for version info) -- NOT a production header, only ever included
// by kfx_script_utest's own test sources. Gives tests an absolute path to
// this repo's config/ directory, needed by lua_base_test.cpp's
// open_lua_script() fixture (docs/refactor/testing/comprehensive/
// stage-06-kfx-script-luajit.md §3): keeper_runtime_directory has to
// point somewhere real before FGrp_FxData paths resolve.
#define KFX_REPO_CONFIG_DIR "@CMAKE_SOURCE_DIR@/config"
