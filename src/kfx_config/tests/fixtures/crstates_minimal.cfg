; Fixture for config_crtrstates_test.cpp -- a minimal crstates.cfg
; exercising load_creaturestates_config_file()'s NamedField mapping.
; PROCESSFUNCTION/CLEANUPFUNCTION/MOVEFROMSLABFUNCTION/MOVECHECKFUNCTION/
; SPRITEIDX are deliberately omitted -- they need config_reload_callbacks'
; function-command tables / a real sprite lookup, neither exercised here.
[state0]
Name = STATE_A
CaptiVe = 1
Transition = 1
FollowBehavior = FollowLeader
StateType = Work
BlocksAllStateChanges = 1
Overrides = FEED SLEEP UNKNOWN_TOKEN
