; Fixture for config_cubes_test.cpp -- a minimal cubes.cfg exercising
; load_cubes_config_file()'s real NamedField/parse_named_field_blocks
; field-mapping, the first worked example of that pattern (a second,
; older-style config family alongside value_util.c's TOML load_toml_file()
; -- see config/fxdata/cubes.cfg for the real, full-size file this is
; modelled on).
[cube0]
Name = CUBE_A
Textures =   1   2   3   4   5   6
Properties = LAVA WATER
