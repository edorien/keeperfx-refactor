; Fixture for config_sounds_test.cpp -- exercises all three sections
; load_sounds_config_file() parses: [system] (SpeechQueueLimit only),
; [sounds] (NAME = numeric_id [count] -- the plain name->ID registration
; path, no real audio file I/O), and [speech] (SMsg name -> override
; path, stored verbatim with no file-existence check).
[system]
SpeechQueueLimit = 7

[sounds]
REFUSAL = 100
GOLD_PICKUP = 200 3

[speech]
LevelWon = speech/custom_levelwon.wav
