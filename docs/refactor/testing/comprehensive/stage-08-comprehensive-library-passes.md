# Stage 8 — Comprehensive per-library passes

See [00-overview.md](00-overview.md) §4 for why this isn't "test
everything." Each library's stage-04* document already named its first
pass's scope explicitly (usually one file, sometimes one function); this
document is the follow-up plan for going meaningfully deeper, library by
library, plus `kfx_sim`'s promised per-cluster breakdown in full.

## 1. What "comprehensive enough" means, absent a coverage number

Stage-05 deliberately defers a coverage-percentage floor. Until that
number exists and has a real baseline, use a cheaper per-library
checklist instead — for each library, comprehensive-enough means:

1. Every function stage-04*'s own document flagged as "a reasonable next
   candidate" (most of them named at least one) actually has a test.
2. At least one function per *major internal cluster* (a library's own
   natural file groupings — `kfx_sim`'s `map_*`/`thing_*`/`creature_*`/
   `room_*`/`player_*`, `kfx_config`'s `config_*` loaders, …) is covered,
   not just whichever cluster happened to contain the easiest pilot
   function.
3. At least one test per library exercises pattern B (a callback-struct
   fake) where the library owns or calls through one — stage 1–4's pilots
   were deliberately chosen to *avoid* pattern B wherever possible to
   prove the harness cheaply first; a comprehensive pass is exactly where
   that avoidance should stop, since the callback-struct seams are a
   real, load-bearing part of this codebase's design and are currently
   the least-tested part of it precisely because they were the hardest
   pilot candidates.

## 2. Per-library follow-up plan

| Library | Tested now (stage-04*) | Concrete next targets | Pattern needed |
|---|---|---|---|
| `kfx_platform` | `bflib_math.c`: `LbSqrL`, `LbLerp`, `LbMathOperation` (the full opkind switch — comparison, logic-as-boolean, bitwise, arithmetic, and the unrecognized-opkind fallback, 4 tests; this is also the entire body of `kfx_game`'s `get_condition_status()`, tested here where it actually lives rather than as a duplicate one-line-wrapper test); `bflib_basics.c`: `get_gameturn` (`GetGameTurnFunc` pattern B — done), `saturate_set_unsigned` (`EmulateIntegerOverflowFunc` pattern B, both the clamp and wrap branches asserted — done); `bflib_string.c`: all six functions (`LbLocTextStringLength`/`_Size`/`_PosToLength`/`_Concat`/`_Insert`/`_Delete`, 11 tests) — entirely pure UTF-8-aware string ops, no extern state, tested with a real 2-byte UTF-8 character throughout to confirm char-position vs. byte-offset handling, not just ASCII; `bflib_planar.c`: `LbSetRect`, `get_angle_difference`/`get_angle_sign` (both masking/wrap-around behavior, including the "short way around" case at the 180° boundary), `get_chessboard_distance`/`_3d_distance` (9 tests); `bflib_video.c`'s `VideoScaleCallbacks` consumer — the third and last known callback consumer, now landed: `scale_value_for_resolution_with_upp` (pure, `units_per_px` is a direct argument), `scale_value_landview` (pattern A on the bare `units_per_pixel_landview` extern), and `scale_value_by_horizontal_resolution`/`_vertical_resolution`/`scale_ui_value`/`scale_fixed_DK_value`/`scale_value_menu` (pattern B, each reading its own field off a faked `VideoScaleCallbacks`, 10 tests total). **Follow-up pass (2026-08-29, per an explicit "focus on the lowest-ranked library" request), 41→175 tests total**: `kfx_memory.c` (full, both the release-build allocator wrappers and the scratch/arena allocator — `KFX_DEBUG_MEMORY` is never defined by this build, so the guard-zone/per-callsite-accounting path stays dead code); `bflib_coroutine.c` (full, 100% — a hand-rolled function-pointer scheduler, plain C function pointers as test doubles, no fake-provider needed; found `CoroutineLoopState`'s `CLS_REPEAT` value matches none of `coroutine_process()`'s three dispatch branches, so it just re-invokes the same queued function next iteration rather than erroring); `bflib_guibtns.c`'s `check_if_pos_is_over_button`/`setup_input_field`; `bflib_keybrd.c`'s `keyboardControl` state machine; `bflib_datetm.cpp`'s pure `LbDateTimeDecode` (TZ pinned to UTC via the fixture) and `get_trigger_time_measurement_fps`; `bflib_fileio.c`'s real-file open/read/write/seek/eof/length/delete lifecycle (found `LbFileOpen(..., Lb_FILE_MODE_NEW)` is broken for any path starting with `/` — `create_directory_for_file()`'s `mkdir("")` on the leading-slash's empty directory component fails with `ENOENT`, dormant since every real caller uses relative paths); `bflib_dernc.c`'s `rnc_crc` (confirmed to be plain CRC-16/ARC against its standard check value), `UnpackM1`'s early return, and a real `LbFileSaveAt`/`LbFileLoadAt`/`calculate_file_checksum` round trip; `custom_zip.c`'s `read_map_zip_entry` validation early-returns (pattern B on `MapZipCallbacks`); `bflib_filelst.c`'s `LbDataFree`/`LbDataLoad`'s `'*'`-wildcard branch (found `LbDataLoadSetModifyFilenameFunction` doesn't actually return the *previous* hook despite reading like it does — it just echoes `newfunc` back); `spritesheet.cpp` (full — a real index+data file pair round-tripped through `load_spritesheet`, matching its private packed `sprite_entry` layout); `bflib_text.c`'s UTF-8/codepage conversion (full non-CJK path — `encode_utf8_codepoint`/`convert_codepage_to_utf8_buffer`/`read_utf_8_codepoint_f`); `bflib_render.c`'s `polyscans[]` alloc/reset/free lifecycle. Per-library: 2.3%→6.1% line coverage, 5.2%→12.7% function coverage. **Deep pass with fixtures (2026-08-30, restarting the ladder from the bottom with an explicit mandate to build fixtures/expose statics rather than stop at the first wall), 175→248 tests total**: this pass's headline finding was that the previous "real OpenAL device" verdict on `bflib_sound.c` was too coarse for the whole file — re-reading every function body found it's almost entirely pure 3D-audio bookkeeping (distance/volume/pan/pitch math, emitter/sample array management via its own `emitter[]`/`SampleList[]`), with real OpenAL calls concentrated in specific branches (`stop_sample`/`play_sample`/`SetSample*`/`GetCurrentSoundMasterVolume`, all still declined). `MaxNoSounds`/`SampleList` were file-scope `static` with no getter, making "sample already playing" scenarios unreachable without the real playback path — un-static'd both (extern in `bflib_sound.h`, the same "expose the module-level global" pattern `kfx_config`'s `campaign` established) and landed ~30 functions this way. `renderer/RendererManager.cpp` (not part of the original pass — added to this library afterward) turned out to guard almost its entire public surface on `s_active_renderer==nullptr`, safely callable end to end at that default; its `RendererDrawCallbacks` seam got a first pattern-B fake. `bflib_vidraw.c`'s `LbSpriteSetScaling*Array`/`LbSpriteClearScaling*Array` (pure fixed-point scan-conversion math into a caller-owned array, no screen buffer) turned out separable from the rest of that 2,117-line file — its first coverage ever; `bflib_mspointer.cpp`'s thin wrappers around them followed for free. `bflib_cpu.c`'s bit-field decoders (`cpu_get_type`/`_family`/`_model`/`_stepping` — pure given a caller-supplied struct, `cpu_detect()` itself is the only real-CPUID call, given a light smoke test since it's benign and always-available, not a "significant harness change"), `bflib_netsession.c`, and `moonphase.c`'s process-start-zero "new moon" default were also picked up. Twelve functions across these files had real external linkage but no header declaration; all added. Per-library: 6.1%→9.5% line coverage, 12.7%→19.5% function coverage. | `bflib_planar.c`'s remaining functions (`distance_with_angle_to_coord_x`/`_y`, `move_coord_with_angle_x`/`_y`, `distance3d_with_angles_to_coord_x`/`_y`, `get_distance_xy`) all depend on `LbSinL`/`LbCosL`/`LbDiagonalLength`'s fixed-point lookup tables for their exact output — deliberately not attempted, since a hard-coded expected value would just be a second, easier-to-get-wrong copy of the table data (same reason `bflib_sound.c`'s `get_emitter_pan`'s general formula branch was also declined this pass). `bflib_video.c`'s `scale_ui_value_lofi`/`scale_fixed_DK_value_by_ar` additionally reach into real screen-dimension state (`LbGraphicsScreenHeight()`), not attempted here. After the deep pass, what's left is narrower and more confidently a real dependency, not just an unexplored one: `bflib_render_trig.c` (4,631 lines)/`bflib_render_gpoly.c`/`bflib_vidraw_spr_*.c`/`bflib_sprfnt.c`'s remaining real-framebuffer drawing code, `bflib_vidsurface.c` (calls `SDL_CreateSurface`/`SDL_BlitSurface` directly, confirmed by reading it), `platform/*.cpp` (real OS window creation), `renderer/RendererSoftware.cpp`/`ITextRenderer.cpp`/`IUIRenderer.cpp` (the real backend `RendererManager.cpp` dispatches to), `bflib_inputctrl.cpp`/`bflib_mouse.cpp`/`bflib_input_joyst.cpp`/`bflib_mshandler.cpp` (real SDL event pump), `bflib_enet.cpp`/`bflib_netsp.cpp` (real sockets), `bflib_sndlib.cpp`/`sound_manager.cpp` (the real OpenAL device `bflib_sound.c` now cleanly stops short of), `bflib_fmvids.cpp` (real Smacker decode), `bflib_crash.c` (signal handlers, unsafe to exercise). | **B** — all three known callback consumers now landed; **A** for the original follow-up pass; deep pass added a new **B** variant (un-static'ing a module-level global for direct fixture access, not just registering a fake callback struct) plus a first `RendererDrawCallbacks` fake |
| `kfx_config` | `config.c`: `parameter_is_number`, `get_id`, the old-style line-oriented config tokenizer (`get_conf_parameter_whole`/`_single`, `recognize_conf_parameter`, `get_conf_parameter_text` — 11 tests, all pure buffer/cursor functions, no `kfx_config_state`; found `recognize_conf_parameter` requires a full token match, a prefix like "FOOBAR" does NOT match a registered "FOO" command); the campaign level-membership predicates (`is_bonus_level`/`is_extra_level`/`is_singleplayer_level`/`is_multiplayer_level`/`is_freeplay_level`/`is_campaign_level`/`is_singleplayer_like_level`, plus `storage_index_for_bonus_level`/`array_index_for_singleplayer_level`/`bonus_level_for_singleplayer_level` — 9 tests, pattern A on the module-level `campaign` global rather than `kfx_config_state`; found `storage_index_for_bonus_level` returns a *compacted* index counting only non-zero prior slots, not `campaign.bonus_levels[i]`'s own array index like its sibling `array_index_for_singleplayer_level` does); the campaign level-navigation functions (`first_singleplayer_level`/`last_singleplayer_level`/`first_multiplayer_level`/`first_extra_level`/`get_extra_level`/`next_multiplayer_level`/`prev_singleplayer_level`/`next_extra_level`/`next_singleplayer_level` — 16 tests; `next_singleplayer_level` is kfx_config's own first pattern-B test, reaching through `game_callbacks->get_intralvl_next_level()`/`clear_intralvl_next_level()`; found the function calls `get_intralvl_next_level()` *twice* — once in the outer `> 0` guard, again to capture the value — so its "negative jump target" branch is unreachable with an idempotent provider and needed a fake that changes its answer between calls to exercise at all); `value_util.c`: `load_toml_file()` against `tests/fixtures/sample.toml` — done, establishing the fixture-file pattern. **Follow-up pass (2026-08-29, immediately after the `kfx_platform` pass above, per the same "move up the ladder, same approach" request), 45→104 tests total**: every `*Callbacks`-registration file's default no-op table exhaustively exercised — `net_callbacks.c` (52 stub functions), `game_callbacks.c` (46), `pathfinding_world.c` (54 — the interface side of the 51-entry `PathfindingWorldCallbacks` seam the "big three" ariadne files still need, *not* that sub-effort itself, just this file's own registration/defaults), `script_hooks.c` (25), `render_overlay.c` (28), `dungeon_availability.c` (10), `sprite_lookup.c` (7) — ~220 tiny `static` functions total, each only reachable through its table's function-pointer fields, driving several of these files to 100%/100% line+function coverage with one exhaustive "call every field" test apiece; `config_campaigns.c` (0/804 lines beforehand — the campaign level-membership predicates above are actually in `config.c`, a different file) got its struct-management functions covered: `clear_campaign`/`clear_level_info`/`free_campaign`, the five `add_*_level_to_campaign` functions, `get_campaign_level_info`/`new_level_info_entry`/`init_level_info_entries`/`grow_level_info_entries`, `is_campaign_loaded`/`is_map_pack`, `init_campaigns_list_entries`/`grow_campaigns_list_entries`/`swap_campaigns_in_list`/`sort_campaigns_quicksort`/`is_campaign_in_list` (four missing header declarations added, same "add the missing declaration" fix used throughout); `config_textures.c`'s `load_textureanim_config_file` — a worked example of the "per-loader TOML-fixture" pattern (a new `textureanim_minimal.toml` fixture), closing part of the open item at right; `config_players.c`'s pure accessors (`player_state_code_name`/`get_player_state_stats`); `config_strings.c`'s pure buffer/lookup functions (`reset_strings`/`fill_strings_list`/`count_strings`/`gui_string`/`cmpgn_string` — `gui_string` needed the missing-declaration fix too). Per-library: 3.3%→8.7% line coverage, 5.0%→32.0% function coverage. | Per-`config_*.c` loaders (`config_crtrmodel.c` 1,635 lines, `config_creature.c` 1,320, `config_magic.c` 563, `config_sounds.c` 477, `config_trapdoor.c` 271, `config_rules.c` 266, `config_settings.c` 260, `config_terrain.c` 227, `config_effects.c` 167, `config_compp.c` 130, `config_slabsets.c` 96, `highscores.c` 94, `config_objects.c` 86, `config_crtrstates.c` 66, `config_spritecolors.c` 64, `config_lenses.c` 55, `config_cubes.c` 40) still untested individually — `load_toml_file()` is the *shared mechanism* every one of them calls, now proven testable with a fixture (`config_textures.c` above is a full worked example), but each of these still needs its own fixture + schema understanding, a volume problem rather than a capability gap. | **A**, **fixture files** (generic loader done, one per-loader schema now landed as an example, the rest open), **B** (`GameCallbacks`, landed via `next_singleplayer_level`, and now every other `*Callbacks` struct in this library) |
| `kfx_pathfinding` | `ariadne_points.c`: point pool; `ariadne_edge.c`: `edge_point_add`/`_get`/`edge_points_clean` (3 tests); `ariadne_naviheap.c`: capacity/empty-state mechanics only (3 tests); `ariadne_tringls.c`: bounds-checked field accessors (`region_id`/`edgelen`/`tree_alt`), `triangle_is_invalid`/`get_triangle`, `link_find`, `triangulation_init_triangles`'s seed state, and the `tri_new`/`tri_dispose` free-list-vs-`ix_Triangles` allocator split (11 tests); `ariadne_navitree.c`: the tag/route bookkeeping functions — `tags_init`'s increment-vs-wrap-and-clear behavior, `store_current_tag`/`is_current_tag`'s round-trip against the live `tag_current`, `copy_tree_to_route`'s `tree_dad[]` walk (including the truncation and start==end edge cases), `update_border_tags`/`border_tags_to_current`'s bounds-checked stamping (9 tests); `ariadne_regions.c`: `region_store_init`/`region_get`/`region_put` (a plain circular queue), `regions_connected`'s bounds/floor-height checks, and `navigation_regions_connected` — the function behind this library's one currently-*unaccepted* `check_layering.py --strict` violation (`player_data.h`, used only for `PLAYERS_COUNT`), root-caused and given a real regression net via a hand-built minimal two-triangle fixture that exercises the actual `region_connect()` BFS, not just early returns (10 tests — see [`docs/refactor/todo/two-remaining-layering-violations.md`](../../todo/two-remaining-layering-violations.md)). **Follow-up pass (2026-08-29, third rung of the "focus on the lowest-ranked library" push, after `kfx_platform` and `kfx_config`), 41→58 tests total** — this library started already reasonably well covered, so the gains closed gaps *within* the files above rather than landing a new one: `ariadne_findcache.c`'s `triangulation_init_cache`/`triangle_brute_find8_near` (the cache-hit lookup path only — `triangle_find8`/`point_find`'s real point-location walk still needs the triangulated fixture noted at right; `triangle_brute_find8_near` had no header declaration, added); `ariadne_tringls.c`'s `get_triangle_point`/`triangle_tip_equals`/`edgelen_set`/`triangle_find_first_used` (the last reusing `triangulation_init_triangles` as a deterministic fixture); `ariadne_navitree.c`'s `navitree_add`/`delaunay_init` (`ix_delaunay` had no header declaration, added — and this file's own prior comment claiming `delaunay_init` needed a real triangulated fixture like its Delaunay-walk siblings turned out to be wrong on inspection: it's just `ix_delaunay = 0`, corrected); `ariadne_naviheap.c`'s actual min-heap priority ordering (`naviheap_top`/`naviheap_remove` popping in ascending `tree_val` order, previously deferred pending `ariadne_navitree.c`'s own fixture, which now exists); `ariadne_points.c`'s `point_dispose`; `ariadne_regions.c`'s `region_set`/`region_unset`/`region_unlock` (their `Regions[]` bookkeeping stays unobservable with no accessor, but `region_set`'s write to the triangle's own region id is, via the already-covered `set_triangle_region_id`). Per-library: 6.9%→9.7% line coverage, 21.3%→28.7% function coverage. | `ariadne_findcache.c`'s `triangle_find8`/`point_find` (the actual point-location triangulation walk via `triangle_divide_areas_s8differ`'s barycentric-sign math) remain untested — reproducing `ariadne_regions_test.cpp`'s "hand-verified minimal fixture" discipline for point-in-triangle geometry instead of region-BFS connectivity was assessed and declined as a bigger, separate undertaking. `ariadne_navitree.c`'s `delaunay_add`/`_add_triangle`/`optimise_heuristic`/`delaunay_seeded` still need a real triangulated area — same reason. The big three (`ariadne.c`, `ariadne_update.c`, `ariadne_wallhug.c` — 3,313/1,773/2,178 lines) need `PathfindingWorldCallbacks` (51-entry, architecture.md §2.2a) fakes driving real pathfinding logic and are a dedicated sub-effort of their own, likely deserving a `stage-08b`-style follow-up document once started rather than a table row -- the interface's own default-table registration/no-op stubs are already covered, in `kfx_config`'s `pathfinding_world_test.cpp`. | small files: **A** (mostly done); big three: **B** (51-entry interface) |
| `kfx_sim` | `map_utils.c`: `small_around_index_in_direction` | See §3 — full per-cluster plan, not a table row. |  |
| `kfx_render` | `light_data.c`: simplest accessors, plus the `lish.lights[]`/`lish.shadow_cache[]` allocators (`light_allocate_light`/`_free_light`/`light_count_lights`, `light_allocate_shadow_cache`/`_shadow_cache_invalid`/`_index`/`_free`) and the `light_get_light_intensity`/`_set_light_intensity` round-trip — found `light_get_light_intensity` refuses to read an unallocated light (unlike bare-field `light_get_light_radius`), and found (via a genuine cross-test-pollution failure, not assumed) that `light_initialise()`'s own cleanup identifies which light to delete via the light's own `.index` field rather than the loop variable, so a light with `LgtF_Allocated` set but a stale `.index` survives every future reset — fixed the one pre-existing test that tripped this by setting `.index` alongside `.flags`, and added an explicit `lish.shadow_cache[]` clear to the fixture since `light_initialise()` only frees shadow-cache slots reachable through a light, never scanning the array directly (13 tests total in this file now, up from 4); `engine_camera.c`: `get_camera_zoom`/`set_camera_zoom`'s view_mode-keyed field dispatch, `update_camera_zoom_bounds`'s clamp, and `view_zoom_camera_in`/`_out`'s ratio-plus-clamp math for both the isometric (`limit_max`/`limit_min`-bounded) and parchment (fixed `[16, 1024]`, ignores the limit params entirely) view modes (9 tests); `engine_redraw.c`: `update_mouse_light()` (3 tests) — targeted at this library's *accepted* `kfx_render -> kfx_net` residual (`get_packet_direct`, `architecture.md` §8.2): the default no-op `sim_feedback->get_history_packet()` always returns `NULL`, so this function's real behavior is only reachable through the residual stub, every time, by default; covers the `cursor_light_idx == 0` no-op guard and both `PCtr_MapCoordsValid` branches (light turns on and positions from the packet's `pos_x`/`pos_y`, or stays off), observed directly via `lish.lights[]`'s flags/position. **Follow-up pass (2026-08-29, fifth rung of the "focus on the lowest-ranked library" push, after `kfx_platform`/`kfx_config`/`kfx_pathfinding`/`kfx_sim`), 25→40 tests total**: finally landed the `LensEffect` C++ class hierarchy this row's own "concrete next targets" column had flagged since the original pass — `MistEffect`/`OverlayEffect`/`DisplacementEffect`/`FlyeyeEffect`'s constructors, `Draw()`, and `Cleanup()` on a never-`Setup()` instance (confirmed safe by reading every body first: each guards its teardown behind `m_current_lens >= 0` or an internally nullptr-checked helper, and each `Draw()` early-returns before touching its `LensRenderContext*` argument, so `nullptr` is a safe stand-in); the `LensEffect` base class's own `GetType`/`GetName`/`SetEnabled`/`IsEnabled`/`LoadAssetWithFallback`-validation; `LuaLensEffect`'s full lifecycle with a `NULL` `lua_State` (skips `RegisterBufferFunctions` and every other Lua-touching branch) plus its pure `SetConfig`/`SetParameter`/`GetParameter` surface. Per-library: 2.1%→3.1% line coverage, 5.4%→11.1% function coverage (function coverage more than doubled — the biggest relative jump of any library in this whole push). | `light_create_light()`'s full allocation path (shadow cache, dynamic-light bookkeeping — deliberately skipped in stage-04d); `engine_camera.c`'s `PVM_FrontView` zoom-out branch (reads `kfx_config_state.frontview_zoom_distance_setting`, a state dependency deliberately deferred — the isometric/parchment branches covered here needed only the `struct Camera` argument); `vidmode.c`'s scaling functions via the `VideoScaleCallbacks` round-trip (pairs naturally with `kfx_platform`'s callback-wrapper tests — same interface, opposite side, still open); `PaletteEffect` (reaches into real player/palette state via `get_my_player()`/`lenses_conf`/`PaletteSetPlayerPalette`); `LuaLensEffect`'s actual Lua-exposed pixel accessors (`LuaGetPixel`/`LuaSetPixel`/`LuaCopyPixel` — `private`, only reachable by registering into a real `lua_State` and replicating the `.cpp` file's unexported `LuaBufferInfo` layout, a fragile shortcut rather than a real fixture); `engine_render.c` (5,618 lines, the 3D rendering engine), `custom_sprites.c`, `cursor_tag.c` (real map/subtile state), and `LensManager.cpp`/`lens_api.c`'s singleton-backed render-target plumbing all need a live rendering surface or real map/player world state, `kfx_platform`'s `bflib_vidraw.c`-style "significant harness change" territory. | mixed: **A** (camera math, `LensEffect` hierarchy — both landed), **B** (light allocation, `VideoScaleCallbacks` still open) |
| `kfx_net` | `net_checksums.c`: `get_thing_checksum`; `packets.c`: `set_packet_action`/`is_packet_empty` (6 tests — a field-list-drift regression check for `is_packet_empty` against every field `set_packet_action` writes, plus `input_lag_turns`, the one field neither function's name would suggest checking); `net_resync.cpp`: `store_localised_game_structure`/`recall_localised_game_structure`'s 17-field round-trip, and `animate_resync_progress_bar`'s pixel math + both early-return guards (the first test to combine three simultaneous pattern-B fakes — `GetGameTurnFunc`, `NetCallbacks`, `RenderOverlayCallbacks` — 4 tests). Targeted at this library's *accepted* `kfx_net -> kfx_game`/`kfx_frontend` residual (`net_resync.cpp`'s raw-blob resync, `architecture.md` §6.2/§8.2) — the violation itself isn't fixable without restructuring netcode, but the rest of the file wasn't covered at all before this. | `packets_input.c`/`packets_cheats.c` still untested — process_*-shaped functions, mostly player/camera/dungeon-control dispatch rather than pure field packing, so a weaker fit than `set_packet_action`/`is_packet_empty` turned out to be. `net_checksums.c`'s other functions (`update_turn_checksums`, `compare_desync_history_from_host`) are more state-heavy (iterate all players) and lower priority. | **A** |
| `kfx_game` | `game_legacy.c`: `game_legacy_get_gameturn`; `lvl_script_conditions.c`: `condition_inactive`'s bounds-checked status-bitflag read (4 tests — found the out-of-range case returns `false`/"not inactive", the opposite of what a fail-safe bounds check would usually return, tested as the function's actual behavior). `condition_inactive`/`get_condition_status` had no header declaration at all (only ever called from within the same file) — added both to `lvl_script_conditions.h` to make them callable from a test, the same "add the missing declaration" fix `value_util.h`'s `extern "C"` guard was. `get_condition_status` itself deliberately NOT retested here: its whole body is `return LbMathOperation(...) != 0`, now covered directly in `kfx_platform`'s `bflib_math_test.cpp`. `game_merge.c`'s `get_extra_level_kind_visibility()` (7 tests) — the stage-04f pilot's deferred candidate, resolved: `is_full_moon`/`is_near_full_moon`/`is_new_moon`/`is_near_new_moon` turned out to be plain `kfx_platform` extern globals (`moonphase.h`), not a callback indirection, so this stayed pattern A on two lower-layer globals (`kfx_config`'s `campaign`, `kfx_platform`'s moon flags) rather than needing pattern B. **Follow-up pass (2026-08-30, seventh rung of the "focus on the lowest-ranked library" push, after `kfx_platform`/`kfx_config`/`kfx_pathfinding`/`kfx_sim`/`kfx_render`/`kfx_net`), 13→25 tests total**: `lvl_script_conditions.c`'s `pop_condition`/`get_script_current_condition`/`set_script_current_condition` (the "pop from a non-empty stack" branch is unreachable from a test -- `condition_stack[]`/`condition_stack_pos` are module-private with no accessor, and only `command_add_condition()`, real script-parsing state, ever pushes onto it); `game_saves.c`'s inter-level creature-transfer bookkeeping (`add_transfered_creature`/`get_transferred_creature`/`clear_transfered_creatures` -- `get_dungeon()`/`dungeon_invalid()` index `kfx_sim_state.dungeon[]` directly with no allocation-flag gate unlike players, so any in-range index just works); `game_heap.c`'s `setup_heap_manager()` real failure path (the "creature.jty" data file genuinely doesn't exist in the unit-test environment, no fake needed) plus `he_alloc`; `sounds.c`'s `reset_ambient_sound_thing_idx`/`ambient_sound_stop`'s invalid-thing early return (reachable via the same reserved-index-0 sentinel `thing_data.c` uses everywhere else). Per-library: 0.3%→0.8% line coverage, 0.6%→2.5% function coverage. | `lvl_script_value.c` — script *value* commands (`script_kill_creatures`, `script_process_value`, …) are all world-state-mutating, a weaker pattern-A fit than `condition_inactive`/`get_extra_level_kind_visibility` turned out to be. `is_primitive_save_version()` (`game_saves.c`) was considered and declined: its threshold is a pointer-difference between two unrelated globals, an inherently unstable value tied to link layout, not a real invariant, so there's nothing meaningful to assert. `reset_heap_manager()` (`game_heap.c`) was declined too: it calls `LbFileClose()` unconditionally on a handle that starts `NULL` in this binary, and `fclose(NULL)` is undefined behavior. The bulk of the library is still open: `lvl_script_commands.c` (3,741 lines, the largest single file in the whole codebase), `console_cmd.c` (1,990), `lvl_script_commands_old.c` (842), `lvl_script.c` (673), `game_saves.c`'s actual save/load chunk I/O, `sounds.c`'s real OpenAL/S3D functions, `main_game.c`, `lvl_script_lib.c`, `game_loop.c`. | **A** |
| `kfx_frontend` | `gui_topmsg.c`: `erstat_inc`; `gui_vscroll.c`: `gui_vscroll_total`/`gui_vscroll_max_offset` (6 tests) — the `save_game_catalogue[]` fixture stage-08 flagged as a prerequisite, built here: `save_game_catalogue` is a raw `extern` pointer (`kfx_game`'s dynamically-`realloc`'d array), not a state-struct field, so the fixture points it at a local static array directly rather than memset-resetting a struct; also pattern A on `kfx_platform`'s `active_menus[]`/`struct GuiMenu` for `menu_is_active(GMnu_SAVE)`'s Save-vs-Load-menu branch. **Follow-up pass (2026-08-30, eighth rung of the "focus on the lowest-ranked library" push, after `kfx_platform`/`kfx_config`/`kfx_pathfinding`/`kfx_sim`/`kfx_render`/`kfx_net`/`kfx_game`), 9→28 tests total**: `kfx_frontend_state.c`'s full raw-blob save/load/reset/size lifecycle (registered on `GameCallbacks`) — `save_frontend_state`/`load_frontend_state` get a real file round trip via the same `kfx_platform` real-scratch-file discipline `bflib_fileio_test.cpp` established; `gui_frontmenu.c`'s pure `active_menus[]` scans (`get_active_menu`/`first_monopoly_menu`/`menu_id_to_number`/`point_is_over_gui_menu` — the last had no header declaration, added); `gui_topmsg.c`'s `erstat_check` (reusing `kfx_platform`'s `GetGameTurnFunc` pattern-B fixture for its `get_gameturn() & 0x07 == 0` turn gate — `erstat_check` itself plus three backing globals, `erstat[]`/`last_checked_stat_num`/`render_onscreen_msg_time`, had no header declarations either, all added) and `is_onscreen_msg_visible`/`show_onscreen_msg`. Per-library: 0.2%→0.6% line coverage, 0.6%→1.7% function coverage. | Genuinely the hardest library for this exercise otherwise, as stage-04g found — the overwhelming majority of remaining files (`frontend.cpp` 2,026 lines, `front_input.c` 1,943, `frontmenu_ingame_tabs.c` 1,856, `front_landview.c`, `frontmenu_ingame_map.c`, `gui_frontbtns.c`, `gui_draw.c`, `gui_parchment.c`, ...) are GUI-widget-array/screen-coordinate/real-menu-state coupled the same way `gui_vscroll.c` was, and don't reduce to a bare-struct pattern-A test the way `kfx_frontend_state.c`/`gui_frontmenu.c` did. | **A** (with the new `save_game_catalogue[]`/`active_menus[]` fixtures, plus a real-file round trip for `kfx_frontend_state.c`), **B** (`GetGameTurnFunc`, reused from `kfx_platform`) |
| `kfx_script` | Bare-`lua_State` helpers (`lua_params_test.cpp`), the real `open_lua_script()` init chain plus `classes/Pos3d.lua` (`lua_base_test.cpp`, `lua_classes_test.cpp`) — 8 tests. **Follow-up pass (2026-08-30, ninth rung of the "focus on the lowest-ranked library" push, after `kfx_platform`/`kfx_config`/`kfx_pathfinding`/`kfx_sim`/`kfx_render`/`kfx_net`/`kfx_game`/`kfx_frontend`), 8→23 tests total**: `lua_utils.c`'s `try_get_c_method`/`try_get_from_methods` (the shared per-class `__index` metamethod helpers) — real Lua stack manipulation against the same bare `luaL_newstate()` fixture `lua_params_test.cpp` already used, deliberately not the heavier full `open_lua_script()` chain since these two functions only need a live stack; `api.c`'s event-subscription bookkeeping (`api_subscribe_event`/`api_unsubscribe_event`/`api_is_subscribed_to_event`/`api_clear_all_subscriptions`) turned out to be pure array management over the module's own `api_subscriptions[]`/`api_sub_count`, with zero socket/game-state involvement despite living in the external HTTP API server file, plus `get_max_flags` (a pure count of `kfx_game`'s `flag_desc[]`). None of these eight functions had a header declaration despite real external linkage — all added to `api.h`, the usual "add the missing declaration" fix. Per-library: 4.6%→6.1% line coverage, 6.4%→8.5% function coverage. | The var-subscription cluster (`api_is_subscribed_to_var`/`api_subscribe_var`/`api_unsubscribe_var`) additionally reaches into `kfx_game`'s `get_condition_value()` (real Dungeon/Thing/PlayerInfo state depending on `valtype`) — declined this pass, a reasonable next increment once that dependency is worth pulling in. The bulk of the library is still open: the `lua_api_*.c` family's actual Lua-exposed bindings, `lua_triggers.c`, `lua_cfg_funcs.c` — each needs either a real Thing/Room fixture behind the Lua call or the full `open_lua_script()` init chain `lua_base_test.cpp` already established. | **A** (`lua_utils.c`, `api.c`'s subscription bookkeeping — both landed), **B** (bare `luaL_newstate()` fixture, reused from `lua_params_test.cpp`) |
| `kfx_apploop` | `display_should_be_updated_this_turn` (pattern A + a fake `LbTimerClock` + pattern B `GetGameTurnFunc` combined) — 4 tests, `stage-07-kfx-apploop-game-process.md`'s original pilot. **Follow-up pass (2026-08-30, tenth and final rung of the "focus on the lowest-ranked library" push, after `kfx_platform`/`kfx_config`/`kfx_pathfinding`/`kfx_sim`/`kfx_render`/`kfx_net`/`kfx_game`/`kfx_frontend`/`kfx_script`), 4→8 tests total**: `find_frame_rate()`/`packet_load_find_frame_rate()`'s threshold-crossing branch (1000ms/5000ms) — both functions were already reached indirectly by the pre-existing tests (via `display_should_be_updated_this_turn`'s fixed-0 fake clock), but only their "still accumulating" branch, since the clock never advanced; made the fake clock settable to reach the "fires, computes `time_delta`" branch too. First attempt split each function's coverage into two `TEST_CASE`s and failed under `--order rand`: within one process, the second test could inherit the first test's leftover accumulator instead of a known baseline, since both functions' statics have no reset accessor — fixed by combining each function's full sequence into one `TEST_CASE`, then verified stable across three more random seeds. `keeper_screen_swap()` turned out safely callable directly despite its name: `RendererPresentFrame()` no-ops on kfx_platform's `s_active_renderer` static, `nullptr` by default and never set in this test binary. `keeper_wait_for_next_turn()` got its one branch reachable without a real wall-clock read (`frame_skip < 0`, no `GNFldD_WaitSleepMode`) — `get_time_tick_ns()` reads `std::chrono::high_resolution_clock` through a macro, not a registered function pointer like `LbTimerClock`, so there's no existing fake-provider seam for the rest of it. Per-library (single file, `game_session_loop.cpp`): 4.5%→8.3% line coverage, 15%→25% function coverage. | `update()` (the per-turn dispatcher, calls into every other layer), `game_loop()`/`wait_at_frontend()`/`keeper_gameplay_loop()` (the top-level orchestration loops — real coroutines/network/rendering calls throughout) are this ladder's own composition root by design (`game_session_loop.h`'s file comment) and don't reduce to a pattern-A/B unit test without either a real wall-clock seam for `get_time_tick_ns()` or a much larger integration-style harness. This closes the ten-library "focus on the lowest-ranked library" push. | **A** (`keeper_screen_swap`, `keeper_wait_for_next_turn`'s safe branch), **B** (settable fake `LbTimerClock`, extending the existing fixture) |

## 3. `kfx_sim`'s per-cluster breakdown

The library-split plan's own precedent for a library this size: stage 6
(the original `kfx_sim` extraction) got a main document plus three
appendices (`stage-06a`/`b`/`c`) rather than one document trying to cover
all 83 files. This plan follows the same shape — five clusters, matching
`kfx_sim`'s own natural file-prefix groupings. **First pass landed across
all five together**, one foundational-accessor layer per cluster rather
than one cluster taken deep — see
[stage-08b-kfx-sim-clusters.md](stage-08b-kfx-sim-clusters.md) for the
full account (25 new tests, three genuine cross-cluster asymmetries found
by reading each cluster's actual accessor bodies rather than assuming
family resemblance).

| Cluster | Files (prefix) | Status | Still open |
|---|---|---|---|
| **map** | `map_data`, `map_blocks`, `map_columns`, `map_ceiling`, `map_events`, `map_locations`, `map_utils`, `slab_data` | 3 tests (`small_around_index_in_direction`, `small_around_index_towards_destination`) | Spiral-search/filter functions (`get_position_spiral_near_map_block_with_filter`, callback-parameter-driven). |
| **thing** | `thing_data`, `thing_list`, `thing_factory`, `thing_stats`, `thing_creature`, `thing_objects`, `thing_shots`, `thing_effects`, `thing_traps`, `thing_doors`, `thing_corpses`, `thing_physics`, `thing_navigate` | 19 tests: `thing_is_invalid`/`thing_exists`/`thing_get` (5, direct — previously only exercised indirectly via `kfx_net`'s checksum tests); `thing_stats.c`'s pure computation functions (14 — `get_radially_decaying_value`/`_growing_value`, `compute_controlled_speed_increase`/`_decrease`, `compute_creature_max_health`, `is_neutral_thing`/`is_hero_thing`) | `thing_stats.c`'s config-dependent `calculate_correct_*` family (reach through `creature_control`/`config_creature`, a bigger increment). `thing_creature.c`/`thing_factory.c`/`thing_physics.c` beyond the base accessors. |
| **creature** | `creature_control`, `creature_instances`, `creature_graphics`, `creature_groups`, `creature_jobs`, `creature_senses`, `creature_battle`, all `creature_states_*` | 18 tests: `creature_control_get`/`_invalid`/`_exists`/`_get_from_thing` (6; found `creature_control_invalid` has no upper-bound check, unlike `thing_is_invalid`); `creature_states.c`'s `can_change_from_state_to()` transition-permission logic (9); `creature_states_tresr.c`'s `creature_able_to_get_salary()` (3, the first per-state file — needed pattern B via `ConfigReloadCallbacks`, not just A) | Still open: 16 of 17 individual `creature_states_*.c` files, including every actual `process_state`/`cleanup_state` implementation and the `FuncIdx` dispatch table that calls into them — `_tresr`'s one helper function was the cheapest possible foothold, not representative of the rest. |
| **room** | `room_data`, `room_util`, `room_list`, `room_entrance`, `room_garden`, `room_graveyard`, `room_jobs`, `room_lair`, `room_library`, `room_scavenge`, `room_treasure`, `room_workshop`, `roomspace*` | 14 tests: `room_get`/`_invalid`/`_exists`; `compute_room_max_health` (pattern A on `kfx_config_state` + `saturate_set_unsigned`'s pattern B); `get_dungeon_sell_user_roomspace`'s `single_subtile_mode` branch — the first test to actually *call* `kfx_sim`'s `get_packet` accepted-residual stub, not just link against it; `count_slabs_*_wth_effcncy`'s seven-function capacity-computation family (pure over `struct Room`, no other state) | `get_dungeon_sell_user_roomspace`'s other three branches (`drag_placement_mode` is where the packet's field *contents*, not just the call, would matter), `get_dungeon_build_user_roomspace`, `roomspace_prediction.c`, every `room_<kind>.c` file. |
| **player** | `player_data`, `player_instances`, `player_utils`, `player_computer*` | 15 tests: `player_invalid`/`_exists`; `players_are_enemies`/`players_are_mutual_allies`'s pure short-circuits (7, found `player_invalid` uses a wholly different sentinel convention — index 0 is a *real* player here, unlike the other four clusters); `player_utils.c`'s `player_has_lost`/`player_cannot_win` (8, every early-return gate plus the one "still winnable" branch, reaching through `get_player_soul_container()` into a real dungeon/`Thing`, pattern A throughout) | `player_utils.c`'s scoring/payday functions (`compute_player_final_score`, `compute_and_update_player_payday_total` — these reach through `sim_feedback`, a bigger increment). `player_computer*` (AI) — orchestration-heavy, stage-08's own "lower priority" call, still true. |

**creature** remains the strongest fan-in/risk argument for the next
increment of *depth* (not breadth) — this pass deliberately went wide
across all five clusters first, the same "prove the foundational layer
everywhere before going deep anywhere" call every other library's
rollout in this plan made.

## 4. Exit criterion

Not "all eight libraries fully covered" — that's not this document's
claim. Per library: §1's three-point checklist satisfied, confirmed
against a real build and `ctest` run the same way every stage-04*
document already did, with `check_layering.py`/`check_layering_symbols.py`
reconfirmed unaffected after each addition (the standing verification
habit every prior stage in this whole plan has followed without
exception). `kfx_sim` additionally needs at least one landed test per
cluster in §3 before being called "comprehensively" rather than
"pilot-only" covered.
